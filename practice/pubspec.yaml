name: practice
description: A Dart application using dart-flow.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flow:
    git:
      url: https://github.com/Moniepoint/dart-flow.git
      ref: main